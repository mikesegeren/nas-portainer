# Copy to .env and adjust for your NAS. Used by docker-compose when deploying stacks.

# Base path for app config/data (e.g. /volume2/docker on UGREEN)
DOCKER_DATA=/volume2/docker

# Media library path (movies, series, downloads)
MEDIA_PATH=/home/Mike/media

# User/group for LinuxServer and similar images (id/groups on your NAS)
PUID=1000
PGID=100

# Timezone
TZ=Europe/Amsterdam

# qbittorrent VPN (NordVPN) – only needed for stacks/qbittorrent
LAN_NETWORK=
VPN_USER=
VPN_PASS=

# Vaultwarden (infra stack) – Bitwarden-compatible password vault
# Generate with: openssl rand -base64 48. Leave empty to disable admin panel.
VAULTWARDEN_ADMIN_TOKEN=
# Set to false after creating your account to block new signups
VAULTWARDEN_SIGNUPS_ALLOWED=true
# Optional: set when behind HTTPS proxy (e.g. https://vault.example.com)
VAULTWARDEN_DOMAIN=

# Obsidian LiveSync (CouchDB) – used by stacks/productivity
# Set a strong username/password pair; used to log in from the LiveSync plugin.
OBSIDIAN_DB_USER=obsidian
OBSIDIAN_DB_PASSWORD=changeme
